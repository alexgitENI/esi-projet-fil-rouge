stages:
  - build
  - test
  - cleanup

build-job:
  tags:
    - windows-shell-true
  stage: build
  script:
    - cd ../
    - docker-compose build
    - docker-compose up -d

unit-test-job:
  stage: test
  dependencies:
    - build-job
  script:
    - pytest  
cleanup-job:
  stage: cleanup
  script:
    - docker-compose down 
